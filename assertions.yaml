# Generated by netsert from clab-evpn-lab-leaf1:6030
# Review and edit as needed

targets:
    - host: clab-evpn-lab-leaf1:6030
      assertions:
        - name: BGP peer 10.0.0.0 is ESTABLISHED
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.0.0.0]/state/session-state
          equals: ESTABLISHED
        - name: BGP peer 10.0.0.0 AFI IPV4_UNICAST is active
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.0.0.0]/afi-safis/afi-safi[afi-safi-name=IPV4_UNICAST]/state/active
          equals: "true"
        - name: BGP peer 10.0.0.4 is ESTABLISHED
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.0.0.4]/state/session-state
          equals: ESTABLISHED
        - name: BGP peer 10.0.0.4 AFI IPV4_UNICAST is active
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.0.0.4]/afi-safis/afi-safi[afi-safi-name=IPV4_UNICAST]/state/active
          equals: "true"
        - name: BGP peer 10.255.0.1 is ESTABLISHED
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.255.0.1]/state/session-state
          equals: ESTABLISHED
        - name: BGP peer 10.255.0.1 AFI L2VPN_EVPN is active
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.255.0.1]/afi-safis/afi-safi[afi-safi-name=L2VPN_EVPN]/state/active
          equals: "true"
        - name: BGP peer 10.255.0.2 is ESTABLISHED
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.255.0.2]/state/session-state
          equals: ESTABLISHED
        - name: BGP peer 10.255.0.2 AFI L2VPN_EVPN is active
          path: bgp[default]/neighbors/neighbor[neighbor-address=10.255.0.2]/afi-safis/afi-safi[afi-safi-name=L2VPN_EVPN]/state/active
          equals: "true"
